#
# axsentis GmbH ACME (let's encrypt) automation toolchain config
#

# Path to external tools
PYTHON="@PYTHON@"
OPENSSL="@OPENSSL@"
PWGEN="@PWGEN@"
WGET="@WGET@"
DIG="@DIG@"

# Dedicated user and group for fetch/renew operations
ACME_USER="letsencrypt"
ACME_GROUP="letsencrypt"

# Location of issuer cert
ACME_ISSUER_CERT="https://letsencrypt.org/certs/lets-encrypt-x3-cross-signed.pem"

# Locations
ACME_DIR="@sysconfdir@"
CERT_DIR="@certdir@"
CHALLENGE_DIR="@challengedir@"
ACCOUNT_KEY="${ACME_DIR}/account.key"

# Only renew certificates that expire in the next X days
MIN_EXPIRE_DAYS=30

# RSA Key size in bits
KEYBITS=4096
