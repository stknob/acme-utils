#
# axsentis GmbH ACME (let's encrypt) automation toolchain config
#

# Path to external tools
PYTHON="@PYTHON@"
OPENSSL="@OPENSSL@"
SETFACL="@SETFACL@"
PWGEN="@PWGEN@"
WGET="@WGET@"
DIG="@DIG@"

# Dedicated user and group for fetch/renew operations
ACME_USER="acme"
ACME_GROUP="acme"

# Locations
ACME_DIR="@sysconfdir@"
CERT_DIR="@certdir@"
CHALLENGE_DIR="@challengedir@"
ACCOUNT_KEY="${ACME_DIR}/account.key"

# Only renew certificates that expire in the next X days
MIN_EXPIRE_DAYS=30

# RSA Key size in bits
KEYBITS=4096
